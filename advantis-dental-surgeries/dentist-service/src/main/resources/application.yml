server:
  port: 8082

spring:
  application:
    name: dentist-service
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5433/dentistdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092  # Kafka KRaft

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
