server:
  port: 8085

spring:
  application:
    name: surgery-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/surgerydb


  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/

kafka:
  bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
